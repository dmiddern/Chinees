name: Apply learning UI changes

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-learning-ui.yml

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Patch application source
        run: |
          python <<'PY'
          from pathlib import Path

          path = Path('src/App.tsx')
          text = path.read_text()

          def replace_once(old, new, label):
              global text
              if old not in text:
                  raise SystemExit(f'Missing patch target: {label}')
              text = text.replace(old, new, 1)

          replace_once('''          <Home
            stats={stats}
            settings={settings}
            todayWords={todayWords}
            todayCompleted={todayCompleted}
            history={dailyHistory}
            onStart={startToday}
            onPracticeDays={startDailySetReview}
            onWrite={() => setTab("write")}
            onToggleLevel={toggleLevel}
            onOpenDay={setSelectedDay}
          />''', '''          <Home
            stats={stats}
            todayWords={todayWords}
            todayCompleted={todayCompleted}
            history={dailyHistory}
            onStart={startToday}
            onPracticeDays={startDailySetReview}
            onWrite={() => setTab("write")}
            onOpenDay={setSelectedDay}
          />''', 'Home call')

          replace_once('''          <WordList
            progress={progress}
            lists={customLists}''', '''          <WordList
            progress={progress}
            lists={customLists}
            activeLevels={settings.levels}''', 'WordList call')

          replace_once('''function Home({
  stats,
  settings,
  todayWords,
  todayCompleted,
  history,
  onStart,
  onPracticeDays,
  onWrite,
  onToggleLevel,
  onOpenDay,
}: {
  stats: { total: number; known: number; learning: number; newCount: number; reviews: number };
  settings: Settings;
  todayWords: Word[];
  todayCompleted: number;
  history: DailySet[];
  onStart: (direction: Direction) => void;
  onPracticeDays: (sets: DailySet[], direction: Direction) => void;
  onWrite: () => void;
  onToggleLevel: (level: HskLevel) => void;
  onOpenDay: (set: DailySet) => void;
}) {''', '''function Home({
  stats,
  todayWords,
  todayCompleted,
  history,
  onStart,
  onPracticeDays,
  onWrite,
  onOpenDay,
}: {
  stats: { total: number; known: number; learning: number; newCount: number; reviews: number };
  todayWords: Word[];
  todayCompleted: number;
  history: DailySet[];
  onStart: (direction: Direction) => void;
  onPracticeDays: (sets: DailySet[], direction: Direction) => void;
  onWrite: () => void;
  onOpenDay: (set: DailySet) => void;
}) {''', 'Home props')

          replace_once('''      <section className="level-selector" aria-label="Actieve niveaus">
        {hskLevels.map((level) => (
          <button
            key={level}
            className={settings.levels.includes(level as HskLevel) ? "active" : ""}
            onClick={() => onToggleLevel(level as HskLevel)}
          >
            HSK {level}
          </button>
        ))}
      </section>

''', '', 'home level selector')

          replace_once('''  function restart() {
    if (!session) return;
    onSessionChange(createLearningSession(session.date, session.wordIds, session.title, session.direction));
  }
''', '''  function restart() {
    if (!session) return;
    onSessionChange(createLearningSession(session.date, session.wordIds, session.title, session.direction));
  }

  function switchDirection(direction: Direction) {
    if (!session || session.direction === direction) return;
    onSessionChange(createLearningSession(session.date, session.wordIds, session.title, direction));
  }
''', 'switch direction function')

          replace_once('''      <div className="direction-indicator" aria-label="Vertaalrichting van deze oefening">
        <span className={exercise.direction === "zh-nl" ? "active" : ""}>
          Chinees → {meaningLanguageLabel}
        </span>
        <span className={exercise.direction === "nl-zh" ? "active" : ""}>
          {meaningLanguageLabel} → Chinees
        </span>
      </div>''', '''      <div className="direction-indicator" aria-label="Vertaalrichting van deze oefening">
        <button
          type="button"
          className={exercise.direction === "zh-nl" ? "active" : ""}
          onClick={() => switchDirection("zh-nl")}
          aria-pressed={exercise.direction === "zh-nl"}
        >
          Chinees → {meaningLanguageLabel}
        </button>
        <button
          type="button"
          className={exercise.direction === "nl-zh" ? "active" : ""}
          onClick={() => switchDirection("nl-zh")}
          aria-pressed={exercise.direction === "nl-zh"}
        >
          {meaningLanguageLabel} → Chinees
        </button>
      </div>''', 'direction buttons')

          replace_once('''function WordList({
  progress,
  lists,
  onSelect,''', '''function WordList({
  progress,
  lists,
  activeLevels,
  onSelect,''', 'WordList destructuring')

          replace_once('''  progress: ProgressMap;
  lists: CustomWordList[];
  onSelect: (word: Word) => void;''', '''  progress: ProgressMap;
  lists: CustomWordList[];
  activeLevels: HskLevel[];
  onSelect: (word: Word) => void;''', 'WordList props type')

          replace_once('''  const [query, setQuery] = useState("");
  const [level, setLevel] = useState<"all" | HskLevel>("all");
  const [newListName, setNewListName] = useState("");''', '''  const [query, setQuery] = useState("");
  const [extraLevels, setExtraLevels] = useState<HskLevel[]>([]);
  const [newListName, setNewListName] = useState("");''', 'WordList state')

          replace_once('''  const selectedList = lists.find((list) => list.id === selectedListId);
  const filtered = useMemo(
    () => searchWords(words.filter((word) => (
      (level === "all" || word.level === level)
      && (!onlySelectedList || !selectedList || selectedList.wordIds.includes(word.id))
    )), query),
    [query, level, onlySelectedList, selectedList],
  );''', '''  const selectedList = lists.find((list) => list.id === selectedListId);
  const selectedLevels = useMemo(
    () => hskLevels.filter((level) => activeLevels.includes(level) || extraLevels.includes(level)),
    [activeLevels, extraLevels],
  );
  const filtered = useMemo(
    () => searchWords(words.filter((word) => (
      selectedLevels.includes(word.level)
      && (!onlySelectedList || !selectedList || selectedList.wordIds.includes(word.id))
    )), query),
    [query, selectedLevels, onlySelectedList, selectedList],
  );

  function toggleExtraLevel(level: HskLevel) {
    if (activeLevels.includes(level)) return;
    setExtraLevels((current) => current.includes(level)
      ? current.filter((item) => item !== level)
      : [...current, level]);
  }''', 'WordList filtering')

          replace_once('''      <div className="filter-chips">
        {(["all", ...hskLevels] as const).map((item) => (
          <button className={level === item ? "active" : ""} key={item} onClick={() => setLevel(item)}>
            {item === "all" ? "Alles" : `HSK ${item}`}
          </button>
        ))}
      </div>''', '''      <div className="word-level-filter">
        <div className="section-heading compact-heading">
          <div>
            <span>HSK-niveaus</span>
            <small>Je ingestelde niveaus zijn standaard geselecteerd. Duid hier tijdelijk extra niveaus aan.</small>
          </div>
          {extraLevels.length > 0 && <button onClick={() => setExtraLevels([])}>Alleen standaard</button>}
        </div>
        <div className="filter-chips">
          {hskLevels.map((item) => {
            const isDefault = activeLevels.includes(item);
            const isActive = isDefault || extraLevels.includes(item);
            return (
              <button
                className={isActive ? "active" : ""}
                key={item}
                onClick={() => toggleExtraLevel(item)}
                aria-pressed={isActive}
                title={isDefault ? "Geselecteerd via Instellingen" : undefined}
              >
                HSK {item}{isDefault ? " · standaard" : ""}
              </button>
            );
          })}
        </div>
      </div>''', 'WordList HSK controls')

          path.write_text(text)
          PY
      - name: Install dependencies
        run: npm ci
      - name: Build application
        run: npm run build
      - name: Commit patched source
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.tsx
          git commit -m "Improve learning direction and HSK selection"
          git push
